import React from "react";
import "./propertyButtons.css";

const CreatePropertyBtn = props => (
    <button type="button" className="btn btn-primary" {...props}>Create New Property</button>
)
export default CreatePropertyBtn;