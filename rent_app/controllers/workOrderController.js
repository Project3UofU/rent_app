const db = require("../models");
const mongoose = require('mongoose');
const utils = require('../utils');

module.exports = {

    // TODO: Update
    addComment: function (req, res) {
        utils.error(res, 422, "Not implemented");
    },

};